import { globalState } from "@/stores/global";
import { ref } from "vue";
import { useRouter, type RouteComponent } from "vue-router";
// const global = globalState();

export default function runTitleAnimation(route) {
//   console.log(route.path);

//   const heading = document.getElementById("pageTitleText");

//   setTimeout(() => {
//     if (route.path !== "/") {
//       console.log(heading);
//       heading?.classList.add("rotate-90", "transform");
//     } else {
//       heading?.classList.remove("moved");
//     }
//   }, 125);
}
